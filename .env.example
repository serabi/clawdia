# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=your-bot-token-here

# Your Telegram user ID (integer). Only this user can interact with the bot.
# Find yours by messaging @userinfobot on Telegram.
TELEGRAM_USER_ID=123456789

# --- Authentication ---
# Clawdia uses your ChatGPT plan (Plus/Pro) via Codex OAuth — no API key needed.
#
# Setup (one-time, on a machine with a browser):
#   1. Install the Codex CLI: npm install -g @openai/codex
#   2. Run: codex login
#   3. Copy ~/.codex/auth.json into the codex-auth Docker volume:
#      docker volume create clawdia_codex-auth
#      docker run --rm -v clawdia_codex-auth:/creds -v ~/.codex:/src:ro alpine \
#        cp /src/auth.json /creds/
#
# The bot reads auth.json and refreshes the access token automatically.
# Path inside the container (override if mounted elsewhere):
# CODEX_AUTH_PATH=/credentials/codex/auth.json

# Fallback: standard OpenAI Platform API key (billed separately from ChatGPT plan).
# Only used if no Codex auth.json is found.
# OPENAI_API_KEY=sk-your-openai-key-here

# OpenAI model to use (default: gpt-5.2)
# Options: gpt-5.2, gpt-5.2-pro, gpt-5-mini, gpt-5-nano
# OPENAI_MODEL=gpt-5.2

# Reasoning effort for the model (default: medium)
# Options: low, medium, high
# REASONING_EFFORT=medium

# Maximum tool call rounds per message (default: 5)
# MAX_TOOL_ROUNDS=5

# Path to MCP server registry config (default: /app/config/mcp_servers.yaml)
# MCP_CONFIG_PATH=/app/config/mcp_servers.yaml
